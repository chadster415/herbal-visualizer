---
phase: 01-fuzzy-disorder-search
plan: 02
type: execute
---

<objective>
Generate ~30 semantic search keywords per disorder using the Claude API and write them to `herbal.disorders.search_keywords`.

Purpose: Populates the keywords column with clinically relevant synonyms, symptom terms, and lay language so searches like "stomach acid" → GERD or "memory loss" → Dementia work correctly.
Output: `scripts/generate-disorder-keywords.ts` (ts-node script); all disorder rows in the local DB have populated search_keywords arrays.
</objective>

<execution_context>
@/Users/chadarmstrong/.claude/plugins/cache/taches-cc-resources/taches-cc-resources/1.0.0/skills/create-plans/workflows/execute-phase.md
@/Users/chadarmstrong/.claude/plugins/cache/taches-cc-resources/taches-cc-resources/1.0.0/skills/create-plans/templates/summary.md
@/Users/chadarmstrong/.claude/plugins/cache/taches-cc-resources/taches-cc-resources/1.0.0/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/01-fuzzy-disorder-search/01-01-SUMMARY.md
@components/SystemView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @anthropic-ai/sdk and create generate-disorder-keywords.ts</name>
  <files>scripts/generate-disorder-keywords.ts, package.json</files>
  <action>
Install the Anthropic SDK as a dev dependency using pnpm (the project's package manager at /opt/homebrew/bin/pnpm):
  /opt/homebrew/bin/pnpm add -D @anthropic-ai/sdk

Then create scripts/generate-disorder-keywords.ts with this structure:

1. **Local Supabase client** (for reads AND writes):
   - Use createClient from @supabase/supabase-js (already installed)
   - URL: 'http://127.0.0.1:54321'
   - Key: the standard local Supabase service_role JWT (bypasses RLS for the UPDATE):
     'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hj04zWl196z0Z0bWs'
   - Set schema: 'herbal' in client options

2. **Anthropic client**: use ANTHROPIC_API_KEY env var (throw if missing)

3. **Fetch all disorders**: SELECT id, name FROM herbal.disorders ORDER BY name

4. **Generate keywords in batches of 10**:
   - For each batch, send ONE call to claude-haiku-4-5-20251001
   - System prompt (static, explains task):
     "You are a medical terminology assistant helping build a search index for an herbal medicine education app. For each disorder provided, generate exactly 30 short search keywords or phrases (2-4 words max each) that cover: common symptoms a patient would describe, alternative medical names, related conditions, affected body parts, and common lay terms. Return ONLY valid JSON: an array of objects with shape {id: number, keywords: string[]}. No explanation, no markdown."
   - User message: JSON array of {id, name} for the batch
   - Parse JSON response; if parse fails, log error and skip batch (don't crash)

5. **Write keywords back**: for each {id, keywords} in parsed response, run:
   supabase.from('disorders').update({ search_keywords: keywords }).eq('id', id)

6. **Progress logging**: log "Batch X/Y done (N disorders)" after each batch

7. **Summary**: log total updated count at end

Use typescript-friendly imports (import not require). Add a top-level try/catch and process.exit(1) on fatal error.

Do NOT use prompt caching — the system prompt is only sent once per batch call, not repeated enough to benefit. Do NOT hardcode any disorder-specific content — the script must work for whatever disorders exist in the DB.
  </action>
  <verify>/opt/homebrew/bin/pnpm exec ts-node --version (confirms ts-node still works after install)</verify>
  <done>Script file exists at scripts/generate-disorder-keywords.ts; @anthropic-ai/sdk appears in package.json devDependencies; no TypeScript errors in the script</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Run the keyword generation script with your Anthropic API key</action>
  <instructions>
Claude has created scripts/generate-disorder-keywords.ts.

Run it from the herbal-visualizer directory:

  ANTHROPIC_API_KEY=your_key_here /opt/homebrew/bin/pnpm exec ts-node scripts/generate-disorder-keywords.ts

The script will:
- Fetch all disorders from your local DB
- Call Claude Haiku in batches of 10 (cost: ~$0.05 total for ~150 disorders)
- Write keywords back to herbal.disorders.search_keywords
- Log progress as it goes

Expected runtime: ~60-90 seconds

When done, verify with this SQL in the Supabase SQL Editor:
  SELECT name, search_keywords FROM herbal.disorders ORDER BY name LIMIT 10;

You should see arrays of 30 keywords per disorder.
  </instructions>
  <verification>search_keywords column is populated (non-empty arrays) for all disorder rows</verification>
  <resume-signal>Type "keywords done" to continue to the human-verify step</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Keyword arrays in herbal.disorders.search_keywords — ~30 terms per disorder generated by Claude Haiku</what-built>
  <how-to-verify>
    In the Supabase SQL Editor, run:
      SELECT name, search_keywords FROM herbal.disorders WHERE name IN ('GERD', 'Constipation', 'Hypertension', 'Insomnia') ORDER BY name;

    Check that:
    1. Each has ~30 keywords (array length)
    2. Keywords are semantically relevant (e.g., GERD should include "acid reflux", "heartburn", "regurgitation")
    3. Keywords are short phrases, not sentences
    4. No disorder has an empty array {}

    If any disorder has bad/empty keywords, you can manually fix with:
      UPDATE herbal.disorders SET search_keywords = ARRAY['keyword1','keyword2',...] WHERE name = 'DisorderName';
  </how-to-verify>
  <resume-signal>Type "approved" if keywords look good, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] @anthropic-ai/sdk in package.json devDependencies
- [ ] scripts/generate-disorder-keywords.ts exists and is valid TypeScript
- [ ] All disorder rows have non-empty search_keywords arrays
- [ ] Spot-check: GERD, Constipation, Hypertension keywords are clinically accurate
</verification>

<success_criteria>
- Script created and runnable via ts-node
- All herbal.disorders rows have search_keywords populated with ~30 relevant terms
- Keywords validated by user spot-check
- Ready for Plan 01-03 (Fuse.js frontend integration)
</success_criteria>

<output>
After completion, create `.planning/phases/01-fuzzy-disorder-search/01-02-SUMMARY.md`:

# Phase 01 Plan 02: Keyword Generation Summary

**Generated and stored ~30 search keywords per disorder using Claude Haiku — DB fully populated.**

## Accomplishments
- Installed @anthropic-ai/sdk (devDependency)
- Created scripts/generate-disorder-keywords.ts
- Ran script; all disorder rows have populated search_keywords

## Files Created/Modified
- `scripts/generate-disorder-keywords.ts` — new keyword generation script
- `package.json` — added @anthropic-ai/sdk devDependency

## Decisions Made
- claude-haiku-4-5-20251001 for cost efficiency (~$0.05 total)
- Batch size 10 to balance API calls vs. response size
- service_role JWT for local DB writes (bypasses RLS)

## Issues Encountered
[Any parse failures, slow batches, or keyword quality issues]

## Next Step
Ready for 01-03-PLAN.md (Fuse.js frontend integration)
</output>
