---
phase: 01-fuzzy-disorder-search
plan: 03
type: execute
---

<objective>
Wire Fuse.js into the disorder search box so it matches against both disorder names and the populated search_keywords arrays.

Purpose: Makes the search box semantically useful — "memory loss" finds Dementia, "stomach acid" finds GERD, "joint pain" finds Arthritis — instead of only matching on the exact disorder name string.
Output: Modified SystemView.tsx with Fuse.js installed and integrated; the search box works with fuzzy/semantic matching.
</objective>

<execution_context>
@/Users/chadarmstrong/.claude/plugins/cache/taches-cc-resources/taches-cc-resources/1.0.0/skills/create-plans/workflows/execute-phase.md
@/Users/chadarmstrong/.claude/plugins/cache/taches-cc-resources/taches-cc-resources/1.0.0/skills/create-plans/templates/summary.md
@/Users/chadarmstrong/.claude/plugins/cache/taches-cc-resources/taches-cc-resources/1.0.0/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/01-fuzzy-disorder-search/01-02-SUMMARY.md
@components/SystemView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install fuse.js</name>
  <files>package.json</files>
  <action>
Install fuse.js as a regular dependency (not dev — it runs in the browser):
  /opt/homebrew/bin/pnpm add fuse.js

Verify the install succeeded before proceeding.
  </action>
  <verify>/opt/homebrew/bin/pnpm list fuse.js shows the package; package.json dependencies includes "fuse.js"</verify>
  <done>fuse.js appears in package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Update SystemView.tsx with Fuse.js semantic search</name>
  <files>components/SystemView.tsx</files>
  <action>
Make the following surgical changes to components/SystemView.tsx:

**1. Add import at the top** (after existing imports):
  import Fuse from 'fuse.js';

**2. Add `search_keywords` to the DisorderListItem interface** (currently lines 15-19):
  interface DisorderListItem {
    id: number;
    name: string;
    sort_order: number;
    search_keywords: string[];   // ← add this
  }

**3. Update the Supabase disorders query** (currently line 91):
  Change:
    supabase.from('disorders').select('id, name, body_system_id, sort_order').order('sort_order'),
  To:
    supabase.from('disorders').select('id, name, body_system_id, sort_order, search_keywords').order('sort_order'),

**4. Update the disorderMap push** (currently line 100):
  Change:
    disorderMap.get(d.body_system_id)!.push({ id: d.id, name: d.name, sort_order: d.sort_order });
  To:
    disorderMap.get(d.body_system_id)!.push({ id: d.id, name: d.name, sort_order: d.sort_order, search_keywords: d.search_keywords ?? [] });

**5. Add a useMemo for the Fuse instance** in the component body, AFTER the existing state declarations (around line 50, before the useEffect calls). This builds one Fuse index over all disorders across all systems, rebuilt only when systems data changes:

  const disorderFuse = useMemo(() => {
    const entries = systems.flatMap((s) =>
      (s.disorders ?? []).map((d) => ({ disorder: d, system: s }))
    );
    return new Fuse(entries, {
      keys: [
        { name: 'disorder.name', weight: 2 },
        { name: 'disorder.search_keywords', weight: 1 },
      ],
      threshold: 0.4,
      ignoreLocation: true,
      minMatchCharLength: 2,
    });
  }, [systems]);

  Add `useMemo` to the existing React import at the top of the file.

**6. Replace the matches computation** (currently lines 318-324):
  Change:
    const matches = disorderSearch.length >= 2
      ? systems.flatMap((system) =>
          (system.disorders ?? [])
            .filter((d) => d.name.toLowerCase().includes(disorderSearch.toLowerCase()))
            .map((disorder) => ({ disorder, system }))
        )
      : [];
  To:
    const matches = disorderSearch.length >= 2
      ? disorderFuse.search(disorderSearch).map((r) => r.item)
      : [];

That is the entire change. Do NOT modify anything else — the dropdown rendering, keyboard navigation, selectMatch function, and all other JSX stay exactly as-is. The matches array shape `{ disorder, system }[]` is identical so no other changes are needed.

IMPORTANT: `ignoreLocation: true` is required — without it Fuse.js penalizes matches not near the start of the string, which breaks keyword matching since keywords are array elements at arbitrary positions. threshold: 0.4 allows moderate fuzziness (0 = exact, 1 = match anything).
  </action>
  <verify>/opt/homebrew/bin/pnpm exec tsc --noEmit (TypeScript check passes with no errors)</verify>
  <done>No TypeScript errors; the six changes above are present; no other lines modified</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Fuse.js semantic disorder search integrated into the "Search disorders…" box. Disorder names still match as before; search now also matches against the 30 keywords per disorder stored in the DB.
  </what-built>
  <how-to-verify>
    1. Start dev server (if not running): /opt/homebrew/bin/pnpm dev
    2. Open http://localhost:3000 in browser
    3. Navigate to the overview showing "All Systems &amp; Disorders"
    4. Test these semantic searches (each should return results):
       - "acid reflux" → should match GERD
       - "memory" → should match a memory/cognitive disorder
       - "joint pain" → should match an arthritis or musculoskeletal disorder
       - "high blood pressure" → should match Hypertension
       - "constipation" → should match Constipation (name match still works)
       - "xyz123" → should show "No matches"
    5. Verify keyboard navigation still works (arrow keys, Enter, Escape)
    6. Verify clicking a result still navigates to that disorder
    7. Open browser console — confirm no JavaScript errors
  </how-to-verify>
  <resume-signal>Type "approved" if search works correctly, or describe which searches failed</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] /opt/homebrew/bin/pnpm exec tsc --noEmit passes
- [ ] fuse.js in package.json dependencies
- [ ] Six targeted changes in SystemView.tsx (import, interface, query, map push, useMemo, matches)
- [ ] Semantic searches tested and working in browser
- [ ] Existing search UX (keyboard nav, click, Escape) unchanged
</verification>

<success_criteria>
- Fuse.js installed and integrated
- Searching "acid reflux" surfaces GERD; "memory" surfaces a relevant disorder
- Exact name matching still works (backward compatible)
- No TypeScript errors introduced
- No regressions in search UX behavior
- Phase 01 complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-fuzzy-disorder-search/01-03-SUMMARY.md`:

# Phase 01 Plan 03: Fuse.js Integration Summary

**Semantic disorder search live — Fuse.js matches against names + 30 keywords per disorder.**

## Accomplishments
- Installed fuse.js
- Updated SystemView.tsx with Fuse index (6 surgical changes)
- Verified semantic searches work in browser

## Files Created/Modified
- `components/SystemView.tsx` — Fuse.js integration (6 changes)
- `package.json` — added fuse.js dependency

## Decisions Made
- threshold: 0.4 (moderate fuzz — exact substring still ranks highest due to weight: 2 on name)
- ignoreLocation: true (critical for keyword array matching)
- useMemo for Fuse instance (rebuilt only when systems data changes, not on every keystroke)

## Issues Encountered
[Any Fuse config tuning needed, TypeScript issues, etc.]

## Next Step
Phase complete. Feature ships.
</output>
