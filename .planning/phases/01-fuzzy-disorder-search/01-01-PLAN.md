---
phase: 01-fuzzy-disorder-search
plan: 01
type: execute
---

<objective>
Add a `search_keywords` column to the `herbal.disorders` table to support fuzzy/semantic search.

Purpose: The keywords array is the data foundation for semantic disorder search — without it, Fuse.js can only match against disorder names, missing synonyms like "acid reflux" → GERD or "memory" → Dementia.
Output: Migration file `115_disorder_search_keywords.sql` that adds the column safely (idempotent, re-runnable).
</objective>

<execution_context>
@/Users/chadarmstrong/.claude/plugins/cache/taches-cc-resources/taches-cc-resources/1.0.0/skills/create-plans/workflows/execute-phase.md
@/Users/chadarmstrong/.claude/plugins/cache/taches-cc-resources/taches-cc-resources/1.0.0/skills/create-plans/templates/summary.md
@/Users/chadarmstrong/.claude/plugins/cache/taches-cc-resources/taches-cc-resources/1.0.0/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@components/SystemView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 115_disorder_search_keywords.sql</name>
  <files>supabase/migrations/115_disorder_search_keywords.sql</files>
  <action>
Create the migration file following the project's migration conventions:
- Start with `SET search_path TO herbal, public;`
- Wrap in a `DO $$ ... END $$;` block
- Use `IF NOT EXISTS` guard on the column so the migration is re-runnable
- Add `search_keywords text[] NOT NULL DEFAULT '{}'` to `herbal.disorders`
- Use `RAISE NOTICE` for feedback
- No grants or RLS needed — this is an ALTER TABLE on an existing table, not a new table

Do NOT add any indexes at this stage — Fuse.js does client-side matching so no DB index is needed on this column.
  </action>
  <verify>Read the file and confirm: (1) search_path is set, (2) IF NOT EXISTS guard is present, (3) column type is `text[] NOT NULL DEFAULT '{}'`, (4) no grants block (unnecessary for ALTER TABLE)</verify>
  <done>File exists at supabase/migrations/115_disorder_search_keywords.sql with correct idempotent SQL</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Run the migration in the Supabase SQL Editor</action>
  <instructions>
Claude has created: supabase/migrations/115_disorder_search_keywords.sql

Open that file, copy the SQL, and run it in your local Supabase SQL Editor (http://127.0.0.1:54400 → SQL Editor).

You should see a NOTICE like: "Added search_keywords column to herbal.disorders"

To verify: run `SELECT id, name, search_keywords FROM herbal.disorders LIMIT 5;` — the column should exist with empty arrays `{}`.
  </instructions>
  <verification>Column exists in herbal.disorders with type text[] and all rows have default value '{}'</verification>
  <resume-signal>Type "migration done" to continue to Plan 01-02</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] File supabase/migrations/115_disorder_search_keywords.sql exists
- [ ] SQL is idempotent (IF NOT EXISTS guard)
- [ ] User confirms migration ran successfully
- [ ] SELECT on herbal.disorders shows search_keywords column with '{}' defaults
</verification>

<success_criteria>
- Migration file created following project conventions
- Column `search_keywords text[] NOT NULL DEFAULT '{}'` exists on `herbal.disorders`
- All existing disorder rows have empty array as default (no data loss)
- Ready for Plan 01-02 (keyword generation script)
</success_criteria>

<output>
After completion, create `.planning/phases/01-fuzzy-disorder-search/01-01-SUMMARY.md`:

# Phase 01 Plan 01: DB Migration Summary

**Added search_keywords text[] column to herbal.disorders — all rows default to empty array.**

## Accomplishments
- Created supabase/migrations/115_disorder_search_keywords.sql
- User ran migration; column confirmed present

## Files Created/Modified
- `supabase/migrations/115_disorder_search_keywords.sql` — new migration

## Decisions Made
- text[] NOT NULL DEFAULT '{}' (not nullable, safe for Fuse.js spread)
- No DB index (client-side Fuse.js doesn't need it)

## Issues Encountered
None

## Next Step
Ready for 01-02-PLAN.md (keyword generation script)
</output>
